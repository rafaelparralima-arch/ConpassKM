let steps=Number(localStorage.getItem('passos-hoje')||0), stride=Number(localStorage.getItem('passada-cm')||75), active=false,lastPeak=0,filtered=9.8;
const $=id=>document.getElementById(id); const render=()=>{$('steps').textContent=steps.toLocaleString('pt-BR');$('km').textContent=((steps*stride)/100000).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2})+' km';$('stride').value=stride;$('strideLabel').textContent=stride;localStorage.setItem('passos-hoje',steps);localStorage.setItem('passada-cm',stride)};
function motion(e){const a=e.accelerationIncludingGravity;if(!a||a.x==null)return;const m=Math.sqrt(a.x*a.x+a.y*a.y+a.z*a.z);filtered=filtered*.82+m*.18;const impulse=Math.abs(m-filtered),now=Date.now();if(impulse>1.15&&now-lastPeak>320){lastPeak=now;steps++;render()}}
$('start').onclick=async()=>{if(active){active=false;window.removeEventListener('devicemotion',motion);$('start').textContent='▶ Iniciar';$('status').textContent='Contagem pausada';return}try{if(typeof DeviceMotionEvent==='undefined')throw Error();if(typeof DeviceMotionEvent.requestPermission==='function'&&await DeviceMotionEvent.requestPermission()!=='granted'){$('status').textContent='Permissão não concedida';return}active=true;window.addEventListener('devicemotion',motion);$('start').textContent='Ⅱ Pausar';$('status').textContent='Contando enquanto o app estiver aberto'}catch{$('status').textContent='Sensor indisponível neste aparelho'}};
$('test').onclick=()=>{steps++;render()};$('reset').onclick=()=>{steps=0;render();$('status').textContent='Contagem zerada'};$('stride').oninput=e=>{stride=Number(e.target.value);render()};render();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');